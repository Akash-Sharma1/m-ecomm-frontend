import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Tile = () => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default Tile;

const styles = StyleSheet.create({});
