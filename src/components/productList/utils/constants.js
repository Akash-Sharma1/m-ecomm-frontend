import { Sizes } from 'styles';

export const ItemSizes = {
  SMALL: {
    height: Sizes.size(270),
  },
  MEDIUM: {
    height: Sizes.size(270),
    minWidth: Sizes.SCREEN_WIDTH * 0.6,
  },
  WIDE: {
    height: Sizes.size(100),
  },
  LARGE: {
    height: Sizes.size(270),
    minWidth: Sizes.SCREEN_WIDTH * 0.8,
  },
};
