// ACTIONS


// DISPATCHERS


// INITIAL STATE
const initialState = {
  userId: '123',
};


// REDUCER
export default (state = initialState, action) => {
  switch (action.type) {
  default:
    return state;
  }
};
