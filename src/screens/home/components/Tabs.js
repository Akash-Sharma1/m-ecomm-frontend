import React from 'react';
import { View } from 'react-native';

import { ProductList } from 'components';

const Tabs = () => {
  return (
    <View>
      <ProductList />
    </View>
  );
};

export default Tabs;
